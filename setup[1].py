from setuptools import setup, find_packages

setup(
    name="langgraph-sql-coder",
    version="0.1.0",
    description="Self-correcting SQL generation with LangGraph",
    packages=find_packages(),
)
